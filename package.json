{
  "name": "dsh-box",
  "version": "1.1.0",
  "private": true,
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "description": "DeepSeek Harness (dsh) desktop shell with tray and managed runtime updates",
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "icons": "node scripts/gen-icons.mjs",
    "build": "cargo build --locked --release --manifest-path src-tauri/Cargo.toml",
    "dist": "cargo build --locked --release --manifest-path src-tauri/Cargo.toml && node scripts/copy-exe.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@tauri-apps/cli": "^2"
  },
  "license": "MIT"
}
