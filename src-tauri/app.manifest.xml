<!--
  Windows app manifest: Common-Controls v6 (tauri default) + PerMonitorV2 DPI awareness.
  The default manifest lacks dpiAwareness, so on high-DPI screens (150%/200%)
  the process is virtualized at 96 DPI and tauri "logical" sizes render as
  physical pixels, making windows ~1/3~1/2 smaller than dsh native UI.
  Keep this file ASCII-only: rc.exe reads the manifest in the system code page.
  Do NOT add an XML declaration line (SxS parser rejects it).
-->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <dpiAwareness xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">PerMonitorV2</dpiAwareness>
    </windowsSettings>
  </application>
</assembly>
