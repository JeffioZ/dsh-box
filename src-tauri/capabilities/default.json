{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DSHBox default capability: core IPC and titlebar dragging",
  "windows": ["main", "tray-menu", "app-dialog"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
