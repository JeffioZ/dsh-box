# DSHBox 1.4.0

本版本在 1.3.0 基础上，重点完成 dsh 0.1.7 全链路适配（新设置存储、Session Log V4、半透明菜单与 Lexical 编辑器），重构启动加载链路修复 dsh 更新后白屏与双重 loading，并将会话统计交还 dsh 原生统计行（余额迁至标题栏），同步加固更新流程与注入链路。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## dsh 0.1.7 适配（本版本主打）

- **设置存储迁移**：dsh ≥0.1.7 将设置从 `settings.yaml` 迁入 `cordis.patch.yml`（两级覆盖、热重载）。DSHBox 按已安装 dsh 版本自动选择存储，语言与主题的读写即时生效，外壳跟随 dsh 界面内主题切换的延迟从最差 3 秒降到 1 秒；修复 alpha 渠道预发布版本（如 0.1.8-alpha）被误判回已废除的 `settings.yaml`、导致语言/主题写入失效的问题；写入语义对齐上游「末条优先」规则。
- **会话日志 V4**：dsh 0.1.7 的 Session Log V4 物理编码与前代兼容，用量统计与每日用量提醒保持正常支持。
- **菜单视觉跟随**：注入 dsh 页面的右键菜单补齐毛玻璃层，对齐 dsh 0.1.7 半透明菜单面，并修复子菜单位置错乱；滚动条对齐上游 5px/pill 规格。
- **编辑菜单适配 Lexical**：dsh 0.1.7 输入框改用 Lexical 编辑器，注入编辑菜单的撤销/重做随之适配。

## 启动加载链路重构

- **修复 dsh 更新后白屏与「两个 loading」**：识别缓存中毒的两种形态（旧页面残留、旧应用假启动），以入口 chunk 指纹比对确认后自动击穿缓存自愈；30 秒兜底上限保留。
- **收幕时机由应用安静度驱动**：遮罩等到页面真实挂载且短时间无变化后才开始退场，避免揭开仍在重排的首帧；遮罩与启动页视觉逐值一致，衔接无断层。
- 修复启动过渡、控制中心弹窗切换与首次打开的闪烁时序。

## 界面调整

- **统计状态栏移除**：会话统计交还 dsh 原生输入框下方的统计行（上游持续迭代、零维护成本），DSHBox 不再复刻；余额 chip 迁至标题栏（`hide_balance` 控制显隐），标题栏常驻显示 `DSHBox x.y.z · dsh a.b.c` 版本信息（悬停可见 Node/npm/端口环境详情）。
- 自绘界面文字规格收敛为统一 token 体系（字号阶梯、弱化文字对比度），启动页字号归一。

## 更新与运行时

- **「改用内置 Node」一键切换**：此前复用系统 Node 的安装，可在检查更新页切换到内置便携 Node（下载安装不停服务，重启后生效），之后即可使用 dsh/npm 自动更新。
- **更新流程加固**：运行时切换加就绪门控防止劫持启动流程；Node 版本判定改用真实探测；更新完成后状态事件可靠送达；备份清理移出关键路径，完成提示先行展示。
- **剪贴板直通**：主窗口与控制中心内粘贴不再弹出浏览器式权限框。

## 稳定性

- **注入链路运行时防御**：注入脚本分段彼此隔离并对组装产物做语法校验，单段异常不再拖垮其余注入功能；修复深色主题首帧预设因 DOM 观察缺失而静默失效的问题。
- 依赖例行更新（tauri 全家族等）与文档勘误（英文 README 过期指引、故障排查密钥入口）。

## 升级说明

- 首次启动本版本无额外操作。统计状态栏的用户请在 dsh 输入框下方查看会话统计（dsh 原生统计行），余额在标题栏查看；`hide_stats_line` / `hide_statusbar` 配置项已随状态栏移除，配置文件中的残留字段会被忽略。
- dsh ≥0.1.7 的设置迁移（`settings.yaml` → `cordis.patch.yml`）由 dsh 自身在升级时完成，DSHBox 自动跟随新存储，无需手动操作。
- 上游 dsh 0.1.7 目前为 rc 预发布；本壳已按其 rc 版本完成兼容性复核。

## 已知限制

- 发布产物尚未签名。Windows SmartScreen、macOS Gatekeeper 或 Linux 安全策略可能要求手动放行。
- Windows 是主要本地测试平台；macOS 与 Linux 依赖完整 CI 矩阵验证。
- dsh 插件与 dsh 共享运行权限。安装第三方插件前仍需自行核实来源。
- 页面增强依赖官方 dsh Web UI 的兼容性；上游大版本变化可能需要同步适配。
- 外部服务模式下无法获取新版 dsh 的进程级 token，新版外部 dsh 的无凭据探测会失败。
