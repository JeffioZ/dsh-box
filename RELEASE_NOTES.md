# DSHBox 1.3.0

本版本在 1.2.0 基础上，重点大幅优化了启动速度与启动观感（实测整机启动约 6 秒降至约 4 秒），新增供应商余额自动识别与关于页构建信息，系统性加固了弹窗体系与更新流程，并收敛设置边界——移除模型配置导入/导出与设置窗口内的 API Key 输入。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## 启动性能与观感（本版本主打）

- **服务启动提速约 1.2 秒**：dsh 安装改用扁平依赖布局（pnpm hoisted），消除 Windows 上符号链接解析开销；现有安装将在下次 dsh 更新时自动迁移，无需任何手动操作。
- **服务引导全程并行**：dsh 服务启动与窗口创建、标题栏/状态栏/托盘装配完全重叠；导航对窗口未就绪的快速路径增加延迟重试，外部服务秒接场景不再丢失导航。
- **Node 运行时探测缓存**：按文件身份（路径 + 大小 + 修改时间）缓存上次探测结果，跳过每次启动的 `node --version` 进程创建。
- **引导期续接遮罩**：服务就绪后切换到 dsh 页面时，加载态视觉无缝延续，消除"空白 → 页面长出来"的断层感，整体观感更接近原生应用。
- **状态栏加载期降噪**：启动过程中统计区与余额不再显示占位/未就绪噪声，数据到达后各自浮现；会话中途服务重启时保留解释性占位。

## 用量与余额

- **供应商余额自动识别**：配置了已知面板域名（passionapi.com、api.orcarouter.ai、api.deepseek.com 等）的路由无论命名如何均可识别；任意域名的 Sub2API 面板在配置密钥后自动探测识别（只读指纹、遵循出站安全策略）。
- **修复状态栏统计偶发消失**：完整适配 dsh 会话日志 V3 代次；页面心跳改走内部协议并跳过空白会话兜底，统计跟随更稳定。

## 设置与密钥

- **移除模型配置导入/导出**：上游 dsh 已原生支持模型接口不兼容场景的配置，自定义路由请直接在 dsh 自己的设置中维护；本应用继续为自定义路由提供余额查询（只读，不受影响）。
- **设置窗口不再收取 DeepSeek API Key**：与 dsh 官方设置重复，且避免"桌面壳收集密钥"的疑虑。密钥入口收敛为首次配置引导与 dsh 官方界面；状态栏与用量页的未配置提示一并指向 dsh 设置。
- 设置相关入口（托盘/标题栏菜单、控制中心导航与标题）统一标注"DSHBox 设置"，与 dsh 官方设置明确区分。
- 修复：首次配置时"开机自启动"写入失败（如注册表被组策略锁定）不再阻断完成引导。

## 界面与体验

- 关于页展示构建时间（按应用语言本地化显示）。
- 注入 dsh 页面的右键菜单键盘焦点指示对齐 dsh 0.1.6 设计（填充替代描边）。
- 修复状态栏统计区点击偶发无响应（空态隐藏替代禁用）。
- 状态栏视觉打磨：垂直光学对齐、浅色状态色对比度、余额悬停说明状态行。
- 检查更新页：更新完成只收尾本次操作；更新通道跨通道发现支持一键切换更高版本。

## 稳定性与依赖

- 弹窗体系加固：锁重构消除互锁、WebView2 键盘焦点修复、UAC 取消语义补全。
- 应用本体更新加固：离线应用预下载、替换失败留痕、可写前置检查。
- 依赖更新：dirs 7、zstd 0.14、ureq 3.4.2 等全量兼容升级（tauri 全家族保持最新）。
- 图标资源统一收敛到单一注册表，便于后续维护。

## 升级说明

- 首次启动本版本无额外操作。dsh 安装布局的扁平化迁移随下一次 dsh 自身更新自动完成（迁移前后均可正常使用，迁移后获得启动提速）。
- 之前在 DSHBox 设置窗口保存过 DeepSeek API Key 的用户无需任何操作：密钥始终存储在 dsh 自己的凭据文件中，本版本移除的只是设置入口；后续更换请通过 dsh 官方界面或首次引导。
- 上游 dsh 0.1.6 尚未正式发布；本版本已按其 alpha 版本完成兼容性复核。
