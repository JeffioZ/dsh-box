# DSHBox 1.2.0

本版本在 1.1.0 基础上，重点加固了配置读写安全与更新/插件事务，完整适配新版 dsh 会话日志，统一三处菜单入口的文案与排序，并系统性收敛了视觉规格与高 DPI 下的一致性。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## 主要改进

- **配置读写安全**：语言、主题、API Key 与模型配置的读写全部改为 YAML 语义化局部修改——保留文件中的注释、引号与结构，写入前整份校验，不支持的结构拒绝写入而非破坏原文件；模型导入增加 ID 合法性校验，导出脱敏覆盖嵌套字段、多行密钥与锚点残留；DeepSeek 全局密钥不再串用给其他供应商；双文件导入（凭据 + 设置）失败自动恢复到原状。
- **用量与余额**：完整适配 dsh 新版会话日志（流内用量计量、重试消耗、明文日志文件）；单个会话日志损坏不再导致整份用量报告失败，CSV / JSON 导出标注数据完整性；任务完成通知覆盖全部会话，Windows 下点击通知直达对应会话；状态栏"当前会话"跟随 dsh 页面中的实际选择；账户查询并发化、缓存优先、配置变更后不发布过期结果。
- **更新与生命周期**：更新事务的锁序与失败恢复加固，更新失败自动回滚并恢复服务；更新准备阶段不再错误显示服务状态；后台发现新版本改为系统通知，不再弹窗打断使用；启动不再自动联网升级 npm；下载停滞 15 秒即超时，取消更及时。
- **插件**：连续批量安装/卸载事务化，单次失败只回滚当次操作；修复跨操作的卸载身份串用（先装 A 后卸 B 不再把 A 误标为用户卸载）；"检查更新"并行化并改用轻量版本查询，明显提速。
- **菜单与界面统一**：主菜单、托盘菜单、弹窗左侧导航三处入口同一文案、同一排序（用量与余额 → 插件 → 设置 → 更新 → 关于）；主菜单补上"用量与余额"入口；移除冗余的"API 余额"弹窗；输入框与按钮描边对齐 dsh 设计令牌；高 DPI 缩放下菜单分隔线粗细一致；修复数字输入框文字在高缩放下的垂直偏移。

## 体验与稳定性改进

- 启动与退出链路优化（就绪过渡收敛、并行探测）；启动、退出与窗口焦点体验修复；开机自启在注册表项缺失时可正确创建。
- 浅色主题下菜单分隔线、输入框描边等可见性提升；错误提示改为常驻可手动关闭；自绘弹窗与全界面设计令牌系统性收敛；标题栏、状态栏图标在高 DPI 下像素级对齐。
- 检查更新页支持其他更新通道更高版本的一键切换（预览版 / 尝鲜版）；状态栏余额 chip、用量页刷新等交互细节修复；跨平台冗余代码清理。

## 升级说明

- 首次启动本版本时，用量统计缓存会自动重算一次（统计格式升级），属正常现象。
- 自定义模型网关（如企业内网路由）的用量统计不受影响；未提供公开余额接口的网关账户卡会标注"不支持余额查询"。
