# DSHBox 1.1.0

本版本在 1.0.1 基础上，重点增强了用量与余额、更新与插件维护，并完成多项安全加固；同时兼容了新版 dsh 的 Web 鉴权变化。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## 主要功能

- **新版 dsh 兼容**：适配 dsh 0.1.2-alpha 起的进程级 token 鉴权——健康检查、页面导航自动携带启动日志中解析出的 token，旧版 dsh 不受影响；同步适配新版 `/api` 鉴权与 RPC 线协议，状态栏实时统计在新版 dsh 下恢复正常；dsh 保存 API Key 后状态栏即时刷新。
- **用量与余额增强**：新增成本估算与更多账户适配器；会话日志按字节偏移增量解析，长会话下的统计开销大幅降低；用量页支持导出 CSV / JSON；通知点击可深链到对应会话，托盘菜单新增用量入口；新增每日用量预警阈值，超过设定值（百万 token）时提醒。
- **安全加固**：内置页面事件载荷增加签名校验，封堵 dsh 页面伪造事件驱动桌面 UI；Web content 与凭据文件拒读白名单收紧；模型配置中的密钥导出脱敏；跨进程写入窗口收窄。
- **更新与升级**：dsh 更新通道新增尝鲜版（alpha）档位，检查更新支持跨通道降级；应用本体与 Node.js 更新下载显示百分比进度；Node 更新的下载阶段前移到停服之前，服务中断窗口缩短为解压时长。
- **插件**：新增插件不兼容诊断、崩溃自愈与冲突引导修复；推荐清单新增 DSH Better Sidebar。
- **Windows 体验**：自实现 Win11 贴边（Snap Layouts）浮层，最大化按钮悬停即可选择分屏布局。

## 体验与稳定性改进

- 图标库统一迁移到 lucide，标题栏图标全面 SVG 化；弹窗改为 owner 级模态，不再全局置顶；新增通用 toast 反馈体系，控件多态与微动效一致性修复。
- 修复 Windows 启动页运行时导航因协议形式导致的静默失效（重启过渡页、错误页恢复正常）；标题栏/状态栏子 WebView 自愈能力强化。
- 修复会话日志递归枚举遗漏导致的用量为 0；开发模式假数据"今天"采样点跨午夜归日修正。
- 应用替换脚本失败会写入 `replace-error.log` 并在下次启动转发，更新恢复流程更稳健；二进制体积经依赖裁剪明显减小。
- 设置页布局与文案细节修复：用量行距、设置项描述防换行、启动页进度条渐隐与文字跳动。
- 文件写入增加跨进程冲突重试；CI 增加 dependabot 依赖分组更新。

## 平台

| 平台 | 架构 | 产物 |
|---|---|---|
| Windows | x64 | `DSHBox.exe` |
| macOS | arm64 / x64 | dmg |
| Linux | x64 / arm64 | zip |

## 已知限制

- 发布产物尚未签名。Windows SmartScreen、macOS Gatekeeper 或 Linux 安全策略可能要求手动放行。
- Windows 是主要本地测试平台；macOS 与 Linux 依赖完整 CI 矩阵验证。
- dsh 插件与 dsh 共享运行权限。安装第三方插件前仍需自行核实来源。
- 页面增强依赖官方 dsh Web UI 的兼容性；上游大版本变化可能需要同步适配。
- 外部服务模式下无法获取新版 dsh 的进程级 token，新版外部 dsh 的无凭据探测会失败。
