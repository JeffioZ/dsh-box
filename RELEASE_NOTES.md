# DSHBox 1.0.0

首个正式版本。DSHBox 是 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（dsh）的跨平台桌面外壳：主界面加载官方 `dsh web`，不改 dsh 内核、不 fork 上游，仅通过注入脚本、读写 `$DSH_HOME` 配置与调用 dsh CLI 三条通道交互。自 0.1.16 起经多轮迭代，本版本为功能与稳定性基线。

## 运行时管理

- 零依赖准备：自动检测并安装 Node.js 与 dsh 包，安装过程在启动页可视化展示（多步骤进度）
- 事务化更新：dsh 与 Node.js 更新采用"备份 + 标记 + 中断恢复"事务，失败自动回滚，被打断时下次启动自动还原
- 服务守护：dsh web 异常自动恢复（看门狗），页面挂起/崩溃自动重载（心跳监控，指数退避）
- 周期检查：运行期每 6 小时自动检查 dsh 新版本，发现后提示（不自动安装）
- 更新可视化：dsh 更新流程带进度与结果状态闭环（成功/失败明确反馈）
- 进程树守卫：退出时清理全部子进程，不留孤儿进程

## 首次使用与配置

- 首次使用引导：设置 API Key、语言、主题与开机自启（可跳过，之后仍可在设置中调整）
- 语言与主题跟随 dsh 设置自动同步，界面中/英双语、深浅色主题随系统切换
- 便携模式：可执行文件旁放置 `portable.txt` 后，数据目录跟随 exe（exe 旁 `data/`），拷 U 盘即用
- 配置项：端口、API Key、API 地址、语言、隐藏工具调用、隐藏统计行、隐藏状态栏（`config.json`，环境变量可覆盖）

## 桌面体验

- 系统托盘常驻、自绘标题栏（主菜单 + 窗口控制）、统一弹窗（余额 / 检查更新 / 插件管理 / 设置 / 关于）
- 底部状态栏：会话统计与 API 余额实时展示
- 余额查询：标题栏余额显示 + 余额弹窗（总额与指标卡），缓存优先、后台刷新
- 任务完成系统通知，点击通知回到窗口
- dsh 页面右键菜单注入，含本地文件操作（如用 VS Code 打开）
- 窗口尺寸记忆；深浅色两套样式与动效统一走设计 token
- 单实例：重复启动自动把参数转交已运行实例

## 插件与扩展

- 内置插件市场：搜索 npm 上的 dsh 插件，一键安装/卸载（走官方 `dsh plugin` CLI），装/卸后自动重启服务生效
- 分类快捷入口：皮肤/主题、工具、工作流预设
- 首次引导预装 `dshmarket` 与 `dsh-file-drop`（均为 MIT 许可），已装插件每 24 小时后台检查 npm 更新

## 应用自更新与分发

- 应用本体自更新（Windows）：检查 GitHub Releases、下载并校验，确认后替换 exe（保留 `.old` 备份、失败回滚）并自动重启
- 新版后台预下载：周期检查发现新版即自动下载，完成提示重启应用
- Windows 缺少 WebView2 时引导安装官方 Runtime

## 平台支持

| 平台 | 架构 | 产物 |
|---|---|---|
| Windows | x64 | 单个 exe，双击即用 |
| macOS | arm64 / x64 | dmg，拖入 Applications 安装（未签名，首次运行需按指引手动放行） |
| Linux | x64 / arm64 | zip，解压即用 |

三平台由 GitHub Actions 自动构建、tag 触发发布，产物全部来自 CI。

## 工程与安全

- 薄外壳边界：不改 dsh 内核、不 fork 上游、不 patch dsh 包
- 更新事务原语（备份 + 标记 + 中断恢复），所有"替换文件"类操作复用
- 会话日志只读解析，不写入 dsh 数据；zstd 解压上限防膨胀、路径边界校验等加固
- 单测覆盖版本解析、更新事务、主题/语言行级合并等核心逻辑（CI 强制 fmt / clippy `-D warnings` / 单测）
