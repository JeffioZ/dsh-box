# DSHBox 1.0.0

> [!NOTE]
> 这是首个公开版本的候选发布说明。正式 Release 只由版本 tag 触发的 GitHub Actions 生成。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## 主要功能

- 首次启动自动检测或安装 Node.js 与 dsh；托管运行时下载按配置使用自动、官方源或镜像策略。
- 安装可随时取消。取消后停留在可重新安装的页面，不会显示为启动失败。
- 管理 dsh 服务的启动、退出、端口冲突、进程树清理、看门狗和页面心跳恢复。
- 可校验并接入已在运行的官方 dsh 服务；外部模式不接管进程或数据，改用本地服务时会恢复未完成的首次设置。
- 提供系统托盘、自绘标题栏与状态栏、任务完成通知、窗口位置记忆、深浅色和中英双语。
- 通过官方 `dsh plugin` CLI 搜索、安装、卸载和更新插件；内置插件只在首次设置明确同意后安装，主动卸载后仍可手动装回，但不会恢复自动更新；社区插件始终由用户手动安装。
- 默认文件插件由 DSH File Drop 更换为 DSH File Upload；旧插件不再预装或推荐，已有安装会按用户此前的选择迁移。
- 导入和导出 `llm-pi-ai` 自定义模型路由，API Key 与普通设置分开保存。
- 用量与余额：会话 token 按日/模型聚合，月历热力图、最近 14 天与按模型下钻；供应商余额与订阅额度统一卡片，后台周期监测、瞬时错误保留上次数据并标记、余额不足 30%/10% 分级预警；状态栏实时显示当前会话统计与余额。
- Windows 支持单文件便携模式，并可检查和安装应用更新。

## 体验与稳定性改进

- 统一主菜单、托盘菜单和页面右键菜单的分组、圆角、描边、阴影、图标与交互状态。
- 修复退出菜单项下方看起来像多余分隔线的问题。
- 修复窗口最小化再恢复后，最小化按钮偶尔保留悬停高亮的问题。
- 窗口缩放改用单一物理像素布局路径，并将几何状态改为停顿后落盘，减少闪白与调整宽度时的卡顿。
- 控制中心按数据、管理和支持重新分组；页面标题、关闭按钮、输入框和滚动条使用统一规范。
- 首次设置在较小逻辑视口中可滚动，并会优先压缩装饰空间，避免高 DPI 屏幕遮住操作项。
- 启动失败页将原始错误折叠到“错误详情”，支持复制错误和打开日志文件夹。
- dsh 与 Node.js 更新使用备份、事务标记和中断恢复；Windows 应用附件按精确 tag 与 SHA-256 摘要校验。

## 平台

| 平台 | 架构 | 产物 |
|---|---|---|
| Windows | x64 | `DSHBox.exe` |
| macOS | arm64 / x64 | dmg |
| Linux | x64 / arm64 | zip |

## 已知限制

- 发布产物尚未签名。Windows SmartScreen、macOS Gatekeeper 或 Linux 安全策略可能要求手动放行。
- Windows 是主要本地测试平台；macOS 与 Linux 依赖完整 CI 矩阵验证。
- dsh 插件与 dsh 共享运行权限。安装第三方插件前仍需自行核实来源。
- 页面增强依赖官方 dsh Web UI 的兼容性；上游大版本变化可能需要同步适配。
