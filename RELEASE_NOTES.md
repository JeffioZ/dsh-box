# DSHBox 1.0.1

> [!NOTE]
> 正式 Release 只由版本 tag 触发的 GitHub Actions 生成。本次发布自 v1.0.0 起累计合入了 59 个提交，涵盖用量与余额整套、模型配置导入/导出、运行时安装降级、更新事务与安全加固等改动。

DSHBox 是 DeepSeek Harness（dsh）的跨平台桌面客户端。主界面使用官方 `dsh web`，桌面端负责准备运行环境、管理服务并提供系统托盘、本地文件操作和更新能力。

## 主要功能

- **用量与余额**：统一弹窗整合账户卡片、订阅额度窗口与历史用量。会话 token 按日/模型聚合，月历热力图、最近 14 天与按模型下钻；供应商余额与订阅额度统一卡片展示，后台周期监测、瞬时错误保留上次数据并标记、余额不足 30%/10% 分级预警。
  - 用量页汇总区同时显示今日、本月、累计 token，以及今日缓存命中率与累计命中率。
  - 月历热力图格子只显示日期号数，token 与命中率移到悬停提示与选中明细，避免格内换行溢出；数值采用千位分隔，汇总区数字过长时悬停可查看完整值。
- **模型配置导入/导出**：首次设置可粘贴 `llm-pi-ai` 段并自动写入凭据；设置页提供卡片式导出入口，导出仅保留非官方目录的自定义路由，API Key 与普通设置分开保存。
- **状态栏余额**：新增余额显示开关；未配置 Key 时提供引导性展示。
- **任务完成通知**：后台轮询会话日志，当前会话出现新的完成轮次且主窗口不可见时发送系统通知。
- **插件**：默认文件插件更换为 DSH File Upload；推荐插件区与插件安装自愈；插件安装/卸载/更新走 `dsh plugin` CLI，多个变更合并为一次服务重启，自动维护等待会话空闲。
- **运行时安装**：dsh 安装支持动态降级到上一个自洽版本，安装进度改为本次增量并避免误报，新增安装故障诊断与 npm 原生编译兜底；便携 Node 捆绑 npm 11 卡死问题在安装后自动升级到 npm 12，npm 检测支持系统 Node、registry 镜像兜底与超时兜底。
- **更新与升级**：检查更新弹窗新增 npm 检测与手动升级；dsh 与 Node.js 更新使用备份、事务标记和中断恢复；Windows 应用附件按精确 tag 与 SHA-256 摘要校验。
- 管理 dsh 服务的启动、退出、端口冲突、进程树清理、看门狗和页面心跳恢复；可校验并接入已在运行的官方 dsh 服务，外部模式不接管进程或数据。

## 体验与稳定性改进

- 修复用量与余额页会话日志枚举：dsh 把会话按启动工作目录存放在嵌套目录，原实现只扫描一层导致 token 用量显示为 0；现改为递归枚举并新增按会话 id 定位真实日志路径的辅助函数，实时 tok/s 与任务完成通知复用同一路径定位。
- 修复状态栏、用量页、账户卡的浅色主题状态与文案一致性；修复菜单触发守卫；统一日历导航、深色主题箭头与日期悬停状态。
- 修复配置解析：line-level YAML 解析、BOM 容错、settings 变更广播；`.credentials.yaml` 按 v1 布局写入并支持原生剪贴板读取。
- 修复启动生命周期与看门狗、窗口状态恢复的健壮性问题；改善对话框过渡稳定性与首次设置滚动表现。
- 安全加固：将 dshd 令牌移出 URL、加固文件操作与输入校验、收紧 capability 与页面协议校验。
- 更新替换脚本改为 BOM 安全写入，更新恢复与 tag 过滤解耦；恢复跨平台 Clippy 兼容并门控 Windows 专属辅助代码。
- 修复退出菜单下方多余分隔线、窗口最小化恢复后的悬停高亮残留、窄屏下首次设置遮挡操作项等问题。

## 平台

| 平台 | 架构 | 产物 |
|---|---|---|
| Windows | x64 | `DSHBox.exe` |
| macOS | arm64 / x64 | dmg |
| Linux | x64 / arm64 | zip |

## 已知限制

- 发布产物尚未签名。Windows SmartScreen、macOS Gatekeeper 或 Linux 安全策略可能要求手动放行。
- Windows 是主要本地测试平台；macOS 与 Linux 依赖完整 CI 矩阵验证。
- dsh 插件与 dsh 共享运行权限。安装第三方插件前仍需自行核实来源。
- 页面增强依赖官方 dsh Web UI 的兼容性；上游大版本变化可能需要同步适配。
