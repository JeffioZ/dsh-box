# DSHBox 1.0.0

正式版本。DSHBox 是 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（dsh）的跨平台桌面外壳（Tauri v2 + Rust），主界面加载官方 `dsh web` 界面，不修改 dsh 内核。自 0.1.16 起经多轮迭代，本版本为功能与稳定性基线。

## 运行时管理

- **零依赖准备**：启动流程分步引导（检测 → 安装 → 启动服务），进度在启动页实时展示；自动安装 Node.js 与 dsh 包
- **可控下载源**：首次安装可选“自动 / 仅官方 / 仅镜像”，自动模式仍按“官方失败 → 镜像”兜底；Node checksum 始终取自官方，并可在安装中取消或切源重试
- **事务化更新**：`updater/transaction.rs` 提供"备份 + 标记 + 中断恢复"原语，dsh 与 Node.js 替换失败自动回滚，更新被打断时下次启动自动还原
- **服务自愈**：看门狗健康检查失败自动重启 `dsh web`；页面心跳监控（10s 轮询、35s 超时判定、指数退避上限约 18.7 分钟），主线程挂起/崩溃自动重载
- **周期检查**：启动时静默检查 + 运行期每 6 小时检查 dsh 新版本（仅提示，不自动安装）；检查更新弹窗内实时展示下载/安装/重启进度与结果（事件即时渲染 + 轮询兜底，竞态不丢结果）
- **进程树守卫**：Windows Job / Unix 进程组，退出时清理全部子进程，不留孤儿

## 首次使用与配置

- **首次引导**：设置 API Key、语言、主题与开机自启，并明确选择是否安装推荐插件（可取消或跳过）
- **与官方 dsh 完全互通**：数据目录使用官方默认 `~/.dsh`（`DSH_HOME` 环境变量可覆盖），与官方 dsh CLI 共享会话、插件与配置
- **语言与主题跟随**：读取 dsh 的 `$DSH_HOME/settings.yaml`（`locale.preference` / `ui-theme.preference`），行级合并原子写入，mtime 门控检测（3s 周期，文件不变零开销）
- **环境变量优先**：`DSH_BOX_ROOT` / `DSH_BOX_PORT` / `DSH_BOX_API_KEY` / `DSH_BOX_API_BASE` / `DSH_HOME` / `DSHD_LANG` 优先于 `config.json`
- **便携模式**：可执行文件旁放置 `portable.txt` 后，数据目录跟随 exe（exe 旁 `data/`），拷 U 盘即用
- **配置分层**：用户设置写 `config.json`，窗口/引导/插件维护状态写 `state.json`，两者不跨文件兜底；API Key 只使用环境变量或 dsh `.credentials.yaml`
- **窗口习惯**：设置页可选择关闭按钮“隐藏到托盘 / 退出应用”和普通启动“显示窗口 / 仅驻留托盘”

## 桌面体验

- 系统托盘常驻（透明托盘菜单，物理像素定位）、自绘标题栏（主菜单 + 窗口控制）、统一弹窗（单窗口左侧导航：余额 / 检查更新 / 插件管理 / 设置 / 关于）
- 底部状态栏：会话统计（轮步 / 耗时 / token，实时 tok/s 尾帧估算，RPC 轮询）+ API 余额 chip；统计组可点击查看完整明细
- 余额查询：复用单例短超时 HTTP agent；瞬时网络错误采用 stale-while-revalidate，未配置/无效密钥立即替换旧余额，并可从状态栏直达本地凭据设置
- 任务完成系统通知：只跟踪当前会话的新 `turn`/`end` 事件，以固定大小的多尾帧读取替代全量解压；读取失败保留 mtime 等待重试。macOS/Linux 点击通知经 `RunEvent::Resumed` 恢复主窗口，Windows 走系统激活与单实例回调
- dsh 页面注入：深色主题首帧预设、右键菜单（含 VS Code 打开本地文件，支持 `~`/MSYS 路径；复制动作有轻提示，危险可执行文件默认打开前确认）、隐藏工具调用与统计行 CSS 注入
- 窗口尺寸记忆；深浅色主题全量 token 化；单实例（重复启动把参数转交已运行实例）

## 插件与扩展

- **内置插件市场**：npm registry 搜索，一键安装/卸载 dsh 插件（走官方 `dsh plugin` CLI）；手动变更可批量完成后统一重启，后台维护等待会话空闲再应用，并在重启后恢复当前会话位置；内置清单由 `resources/builtin-plugins.json` 维护
- 分类快捷入口：皮肤/主题、工具、工作流预设
- 经首次配置明确同意后预装 `dshmarket` 与 `dsh-file-drop`：已装包每 24 小时检查 npm 最新版，落后时后台自动升级并等待空闲应用；失败静默重试，不阻塞启动；pnpm virtual store 错位按文件元数据根因检测并自动备份重建 node_modules 自愈（不依赖 pnpm 报错措辞，DSH_HOME 迁移/复制后同样可靠）
- 插件管理页手动检查/更新；自动与手动更新均绑定检查到的精确版本并校验安装结果，发布冷却状态明确可见；virtual-store 自愈带中断恢复标记；设置页可关自动升级

## 更新通道

- **dsh 更新通道切换**：稳定版（`latest`，推荐日常使用）与开发者预览（`next`，可能包含破坏兼容性的变更），切换后立即重新检查更新，无需重启
- dsh 首装与更新先解析通道的精确目标版本；稳定目标的降级链不混入预览版，安装后再次校验实际版本
- Node.js “自动”下载源在建连、响应体读取或 SHA-256 校验失败时都会完整回退镜像，本地写入/取消错误不盲目换源
- **应用本体自更新（Windows）**：GitHub Releases atom 查询版本；从精确版本 tag 解析唯一附件及 SHA-256 digest，下载到 `.part` 并校验后再替换 exe（保留 `.old` 备份、失败回滚）→ 自动重启
- 新版后台预下载：周期检查发现新版即自动下载，完成提示重启应用
- **WebView2 校验**：注册表读取实际版本，缺失或过旧时下载官方引导安装器自动安装

## 平台支持

| 平台 | 架构 | 产物 |
|---|---|---|
| Windows | x64 | 单个 exe，双击即用 |
| macOS | arm64 / x64 | dmg，拖入 Applications 安装（未签名，首次运行需按指引手动放行） |
| Linux | x64 / arm64 | zip，解压即用 |

五平台由 GitHub Actions 自动构建，tag 触发发布，产物全部来自 CI。

## 工程与安全

- 更新事务原语（备份 + 标记 + 中断恢复），所有"替换文件"类操作复用；应用附件同时约束 tag、下载路径、大小、PE 文件头与 SHA-256
- 会话日志只读解析（zstd 解压设上限防内存膨胀）；`dshd://` 协议要求随机会话令牌、可信窗口与可信 origin，页面完成菜单初始化后清除全局令牌
- `settings.yaml` / `.credentials.yaml` 采用加锁读改写与唯一临时文件原子替换，Unix 新文件权限为 `0600`；模型导入拒绝额外顶层配置和非法凭据名
- 项目一致性检查覆盖全部受控文件的 JS/HTML/i18n/版本/配置/文档链接/图标/右键契约；CI 继续强制 fmt / clippy `-D warnings` / 单测（五平台）
